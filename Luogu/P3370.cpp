#include <cstdio>
#include <iostream>

const int Maxm=1505, Maxn=10005;
char s[Maxm];
bool h[Maxn];

int main(){
	scnaf("%d", n);
	for(int i=0; i<n; i++){
		scanf("%s", s);
		ha=hash(s);
		if(h[ha]) continue;
		h[ha]=true; cnt++;
	}

	return 0;
}

int hash(char* a){
	len=strlen(a);
	for(int i=0; i<len; i++){
		
	}
}
