#include <cstdio>
#include <iostream>

int t;
const double Esp=1e-10;
double h1, h2, d, r, l;

int main(){
	scanf("%d", &t);
	for(int tt=0; tt<t; tt++){
		scanf("%lf %lf %lf", &h1, &h2, &d);
		while(r-l<Esp){
			mid=(l+r)/2;
			mmid=(mid)+r/2;
		}
	}

	return 0;
}
